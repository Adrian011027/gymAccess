asgiref==3.11.1
colorama==0.4.6
Django==6.0.6
django-cors-headers==4.9.0
djangorestframework==3.17.1
djangorestframework_simplejwt==5.5.1
pillow==12.2.0
PyJWT==2.13.0
qrcode==8.2
sqlparse==0.5.5
tzdata==2026.2

# --- Producción ---
# psycopg 3 reemplaza a mysqlclient, que estaba aquí para un bloque de MySQL
# comentado que nunca se usó y que además exige compilador para instalarse.
# El extra [binary] trae las ruedas precompiladas: sin él haría falta libpq-dev.
psycopg[binary]==3.2.10
gunicorn==23.0.0
